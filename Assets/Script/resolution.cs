using System.Collections;
using UnityEngine;

public class resolution : MonoBehaviour {

	// Use this for initialization
	void Start () {
		Camera.main.aspect = 480f / 320f;
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
